[project]
name = "gimbal-target-tracking"
version = "0.1.0"
description = "2D云台打靶视觉识别系统"
requires-python = ">=3.10"
dependencies = [
    "opencv-python>=4.12.0",  # 核心：视觉处理算法库
    "numpy>=1.26.0",          # 核心：矩阵和数学计算
    "pyserial>=3.5",          # 核心：给 STM32 发送数据的串口库
]

